import {View} from 'react-native';
import styled from 'styled-components';

const ReleasesScreenWrapper = styled(View)`
  flex: 1;
  align-items: center;
  justify-content: center;
`;

export {ReleasesScreenWrapper};
